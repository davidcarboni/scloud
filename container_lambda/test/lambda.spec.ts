import { expect } from 'chai';
import { describe, it } from 'mocha';
// import * as l from '../src/lambda';

describe('Some test', () => {
  it('Should meet expectation', () => {
    expect(undefined).to.be.undefined;
  });
});
