import { expect } from 'chai';
import { describe, it } from 'mocha';

// chai.use(require('chai-as-promised'));

describe('s3.ts', () => {
  it('This package contains no tests because there\'s no testable code - these are just wrapper funcitons for aws sdk v3.', () => {
    expect(true).to.be.true;
  });
});
