import { expect } from 'chai';
import * as chai from 'chai';
import { describe, it } from 'mocha';
import * as lambda from '../src/lambda';

chai.use(require('chai-as-promised'));

describe('Handle messages', () => {
  it('Should handle a messge', () => {
    // ...
  });
});
